# Fill in the code to complete the following function for computing factorial
def factorial(n):
    if n == 0: # Base case
        return 1
    else: return "_____________________" # Recursive call
# n * (n - 1)
# n
# factorial(n) * n
# factorial(n - 1) * n

# What are the base cases in the following recursive function?
def xfunction(n):
    if n > 0:
        print(n % 10)
xfunction(n // 10)
# n > 0
# n <= 0
# no base cases
# n < 0

# Fill in the code to complete the following function for computing factorial
def fib(index):
    if index == 0: # Base case
        return 0
    elif index == 1: # Base case
        return 1
    else: # Reduction and recursive calls
        return "_________________________"
# fib(index - 1)
# fib(index - 2)
# fib(index - 1) + fib(index - 2)
# fib(index - 2) + fib(index - 2)